import React from "react";

const Table = props => {
  const { data, loading } = props;
  return (
    <table>
      <thead>LOL</thead>
      <tbody>NOOOOO</tbody>
    </table>
  );
};

export default Table;
